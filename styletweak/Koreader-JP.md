### Furigana small font 

highlight text before create css file
if html use `<rubyBox T="rtc">`

Create `koreader/styletweak/smaller_ruby_text.css` with:

```rubyBox[T="rtc"] {
    font-size: 70% !important;
}
```
Or
```rt {
    font-size: 0.7em !important;
}
```

## note
this markdown isn't related in dictionary 
